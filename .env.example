# RunPet App local env example
# Use with PowerShell:
#   Get-Content .env.example | ForEach-Object {
#     if ($_ -match '^\s*#' -or $_ -match '^\s*$') { return }
#     $k,$v = $_ -split '=',2
#     Set-Item -Path "Env:$k" -Value $v
#   }

APP_ENV=dev
# Optional override for API endpoint
# API_BASE_URL=https://api.example.com

